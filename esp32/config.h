#ifndef CONFIG_H
#define CONFIG_H

// WiFi Credentials
const char* WIFI_SSID = "your_SSID";
const char* WIFI_PASSWORD = "your_PASSWORD";

// Target IP Address
const IPAddress OUT_IP(0, 0, 0, 0);

// OSC Port
const unsigned int OUT_PORT = 8000;

#endif

